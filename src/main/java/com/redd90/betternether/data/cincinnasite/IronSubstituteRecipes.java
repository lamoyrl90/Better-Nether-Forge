package com.redd90.betternether.data.cincinnasite;
/*
import java.util.Collection;
import java.util.Iterator;
import java.util.List;

import net.minecraft.item.Items;
import net.minecraft.item.crafting.ICraftingRecipe;
import net.minecraft.item.crafting.IRecipe;
import net.minecraft.item.crafting.IRecipeType;
import net.minecraft.item.crafting.Ingredient;
import net.minecraft.item.crafting.RecipeManager;
import net.minecraft.item.crafting.ShapedRecipe;
import net.minecraft.item.crafting.ShapelessRecipe;
import net.minecraftforge.common.Tags;
*/
public class IronSubstituteRecipes {
/*
	public static RecipeManager recipeManager;
	public static Collection<IRecipe<?>> recipes;
	private static Collection<IRecipe<?>> newRecipes;
	private static Ingredient ironIngot = Ingredient.fromItems(Items.IRON_INGOT);
	private static Ingredient ironIngotTag = Ingredient.fromTag(Tags.Items.INGOTS_IRON);
	
	public static void scanRecipes() {
		Iterator<IRecipe<?>> iterator = recipes.iterator();
		iterator.forEachRemaining((recipe) ->
		
		{List<Ingredient> ingredients = recipe.getIngredients();
		
		if (recipe.getType() == IRecipeType.CRAFTING) {
			
		} else { return; }
		
		if (recipe.getRecipeOutput().getItem().getRegistryName().getPath().contains("iron"))
			return;
		
		if(ingredients.contains(ironIngot) || ingredients.contains(ironIngotTag)) {
			
		}
		
		});
	}
	*/
}
